<div class="actions">
<%= link_to 'New Subject', new_subject_path %>
</div>

<h1>Subjects' list</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @subjects.each do |subject| %>
  <tr>
    <td><%= subject.name %></td>
    <td><%= subject.description %></td>
    <td><div class="actions"><%= link_to 'Show', subject %></div></td>
    <td><div class="actions"><%= link_to 'Edit', edit_subject_path(subject) %></div<</td>
    <td><div class="actions"><%= link_to 'Destroy', subject, :confirm => 'Are you sure?', :method => :delete %></div<</td>
  </tr>
<% end %>
</table>

<br />

